nginx-deploy:
  enabled: true
  image:
    repository: nginx
    tag: latest
    pullPolicy: IfNotPresent


tomcat-deploy:
  enabled: true
  image:
    repository: tomcat
    tag: stable
    pullPolicy: IfNotPresent
  # Service:
  #   type: nodePort
