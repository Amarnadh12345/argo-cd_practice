apiVersion: v1
kind: Service
metadata:
  name: {{ include "tomcat-deploy.fullname" . }}
  labels:
    {{- include "tomcat-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tomcat-deploy.selectorLabels" . | nindent 4 }}
